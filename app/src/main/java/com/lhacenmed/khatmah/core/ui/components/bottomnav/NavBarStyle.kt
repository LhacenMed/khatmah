package com.lhacenmed.khatmah.core.ui.components.bottomnav

/**
 * Visual style for [BottomNavBar] tab buttons.
 *
 *  [M2] — unbounded ripple that overflows the nav bar top edge (original behaviour).
 *  [M3] — pill/capsule indicator behind the icon; ripple is bounded to that shape.
 */
enum class NavBarStyle { M2, M3 }
