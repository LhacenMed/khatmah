<?xml version="1.0" encoding="utf-8"?>
<!-- Book reader overflow menu — Quran Android's quran_menu.xml subset: night reading,
     search, settings. All shown in the overflow dropdown. -->
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_night_mode"
        android:checkable="true"
        android:icon="@drawable/ic_night_mode"
        android:title="القراءة الليلية"
        app:showAsAction="never" />

    <item
        android:id="@+id/menu_search"
        android:icon="@drawable/ic_search"
        android:title="بحث"
        app:showAsAction="never" />

    <item
        android:id="@+id/menu_settings"
        android:icon="@drawable/ic_settings"
        android:title="الإعدادات"
        app:showAsAction="never" />
</menu>
