<?xml version="1.0" encoding="utf-8"?>
<!--
  A closing note under the last section — a caveat about the rows above rather than a row of its
  own, so it carries no press state and is set at the text's own weight.

  The id is the framework's title, so the note is written as the preference's `android:title` and
  needs no binding code.
-->
<TextView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@android:id/title"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="16dp"
    android:paddingTop="16dp"
    android:paddingBottom="24dp"
    android:textAppearance="?attr/textAppearanceBodySmall"
    android:textColor="?attr/colorOnSurfaceVariant"
    tools:text="تنبيه حول المنبهات" />
