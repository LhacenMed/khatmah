<?xml version="1.0" encoding="utf-8"?>
<!--
  A preference row: icon, title, and the row's current value at the far end.

  The framework stacks the summary under the title, which suits a sentence explaining a setting but
  not a value — a time, a language, a mushaf name. Set opposite the title, a value reads as the
  answer to it, and the row stays one line tall instead of two.

  The ids are the framework's: it binds title, summary, icon and the widget frame by id, so this
  layout can be swapped in for any preference type — plain rows, switches, and the list dialog
  alike — without any of them knowing.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:clipToPadding="false"
    android:gravity="center_vertical"
    android:minHeight="48dp"
    android:orientation="horizontal"
    android:paddingHorizontal="16dp"
    android:paddingVertical="10dp">

    <LinearLayout
        android:id="@android:id/icon_frame"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingEnd="16dp">

        <!-- Fixed box, so a 48dp vector and a 24dp one occupy the same room; the drawable is
             scaled to fit and tinted by the screen (see tintIcons). -->
        <ImageView
            android:id="@android:id/icon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:contentDescription="@null"
            android:scaleType="fitCenter" />
    </LinearLayout>

    <TextView
        android:id="@android:id/title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:ellipsize="end"
        android:maxLines="2"
        android:textAppearance="?attr/textAppearanceBodyLarge"
        android:textColor="@color/pref_title_text" />

    <TextView
        android:id="@android:id/summary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textAppearance="?attr/textAppearanceBodyMedium"
        android:textColor="@color/pref_summary_text" />

    <!-- Switches, and the count pills on the wird rows, land here. -->
    <LinearLayout
        android:id="@android:id/widget_frame"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:gravity="center_vertical"
        android:orientation="vertical" />
</LinearLayout>
